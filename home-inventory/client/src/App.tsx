/**
 * 前端页面尚未实现。
 * 当前仅保留可启动的 Vite + React 工程骨架，后续再做入库/出库 UI。
 */
export default function App() {
  return (
    <main className="placeholder">
      <p className="brand">家物仓</p>
      <h1>项目骨架已就绪</h1>
      <p>前端页面待下一步实现。后端 API 与仓库环境可先独立开发与部署。</p>
    </main>
  );
}
